steps:
  # Step 1: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/box-subscription-system/subscription-backend:latest'
      - '-t'
      - 'gcr.io/box-subscription-system/subscription-backend:latest'
      - '.'

  # Step 2: Push image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/box-subscription-system/subscription-backend:latest'

  # Step 3: Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'subscription-backend'
      - '--image=gcr.io/box-subscription-system/subscription-backend:latest'
      - '--region=us-central1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--add-cloudsql-instances=box-subscription-system:us-central1:subscription-db'
      - '--set-env-vars=NODE_ENV=production,PORT=8080,DATABASE_HOST=/cloudsql/box-subscription-system:us-central1:subscription-db,DATABASE_PORT=5432,DATABASE_USER=nestjs_user,DATABASE_NAME=subscription_db'
      - '--set-secrets=DATABASE_PASSWORD=database-password:latest,JWT_SECRET=jwt-secret:latest,STRIPE_SECRET_KEY=stripe-secret-key:latest'
      - '--max-instances=10'
      - '--min-instances=0'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--timeout=300'

# Store images
images:
  - 'gcr.io/box-subscription-system/subscription-backend:latest'

# Build timeout
timeout: 1200s
